#include <string.h>

main(int argc, char const *argv[])
{
    char *str = "cmd_upload";
    char str2[] = "cao" ;
    int len = strlen(str) ;
     strncmp("", str, strlen(str));
     printf("str = %s strlen = %d\n", str, len ) ;

     strcat(str2, "bin") ;
     printf("%s\n", str2) ;
    /* code */
    return 0;
}
